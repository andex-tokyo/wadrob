# Images

原本をprivate R2へ先に保存し、登録は画像処理を待たない。Android端末上のONNXモデルで服をセグメント化するため、白背景だけでなく床、木目、カーペットにも対応する。結果を4:5のオフホワイトcanvasに中央配置し、960px表示画像と360px thumbnailをR2へ戻す。

ONNX処理が使えない場合は色が均一な背景だけを保守的に除去して正規化する。全処理失敗時は原本を表示する。詳細画面では原本と処理画像を切り替え、再処理できる。原本・表示用・thumbnailは別R2 keyで保持する。
