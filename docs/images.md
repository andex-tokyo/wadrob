# Images

原本をprivate R2へ先に保存し、登録は画像処理を待たない。端末上で向きを補正し、前景の外接矩形で切り出して4:5のオフホワイトcanvasに中央配置し、960px表示画像と360px thumbnailをR2へ戻す。

背景除去（ONNX）は精度・サイズ・クラッシュリスクの理由で削除した（ADR-006）。均一な明るい背景のときだけ保守的に除去する。処理に失敗した場合は原本を表示する。詳細画面では原本と整えた画像を切り替え、再処理できる。原本・表示用・thumbnailは別R2 keyで保持する。
